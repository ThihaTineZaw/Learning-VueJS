{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  // declare types for the router to avoid TS errors when using `useRouter` and `useRoute`.
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue", "typed-router.d.ts"],

  "exclude": ["src/**/__tests__/*"],
  "compilerOptions": {
    // Extra safety for array and object lookups, but may have false positives.
    "noUncheckedIndexedAccess": true,
    // Use the bundler module resolution strategy to avoid issues with Vite's aliasing.
    "moduleResolution": "Bundler",
    // Path mapping for cleaner imports.
    "paths": {
      "@/*": ["./src/*"]
    },

    // `vue-tsc --build` produces a .tsbuildinfo file for incremental type-checking.
    // Specified here to keep it out of the root directory.
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo"
  }
}
